// lib/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  // Main Color
  static const Color main = Color(0xFF5E69EE);

  // Secondary Color
  static const Color secondary = Color(0xFFF4F4FB);

  // Accent Color
  static const Color accent = Color(0xFF39AFEA);

  // Text Colors
  static const Color textPrimary = Color(0xFF2E3A59); // Untuk teks utama
  static const Color textSecondary = Color(0xFF6C7A9C); // Untuk teks sekunder

  // Status Colors
  static const Color success = Color(0xFF4CAF50); // Untuk status sukses
  static const Color error = Color(0xFFE53935); // Untuk status error
}
